[build]
  publish = "."
  # Pas de build nécessaire - site statique pur

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Désactiver la détection automatique de build
[build.environment]
  NODE_VERSION = "18"
